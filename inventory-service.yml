server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory_db
    username: root
    password: root123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

message:
  text: Inventory Service Running

inventory:
  max-stock: 500
  low-stock-alert: 20
