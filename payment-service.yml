server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_db
    username: root
    password: root123

message:
  text: Payment Service Running

payment:
  currency: INR
  gateway: Razorpay
  timeout: 30
